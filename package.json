{
	"name": "@jobmatchme/bee-gate-test",
	"version": "0.1.2",
	"description": "Local fake Bee Dance agent for testing Bee Gate integrations",
	"type": "module",
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"homepage": "https://github.com/jobmatchme/bee-gate-test",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/jobmatchme/bee-gate-test.git"
	},
	"bugs": {
		"url": "https://github.com/jobmatchme/bee-gate-test/issues"
	},
	"publishConfig": {
		"access": "public",
		"registry": "https://registry.npmjs.org/"
	},
	"keywords": [
		"agents",
		"testing",
		"gateway",
		"protocol",
		"nats"
	],
	"files": [
		"dist",
		"LICENSE",
		"README.md"
	],
	"scripts": {
		"clean": "shx rm -rf coverage dist",
		"build": "tsgo -p tsconfig.build.json",
		"dev": "npm run build && node --enable-source-maps ./dist/main.js",
		"start": "node ./dist/main.js",
		"lint": "biome check --error-on-warnings .",
		"lint:fix": "biome check --write --error-on-warnings .",
		"format": "biome format --write .",
		"typecheck": "tsgo --noEmit -p tsconfig.json",
		"test": "vitest run --coverage",
		"test:watch": "vitest",
		"check": "biome check --write --error-on-warnings . && tsgo --noEmit -p tsconfig.json && vitest run --coverage && npm run build",
		"prepare": "husky",
		"prepublishOnly": "npm run clean && npm run check"
	},
	"dependencies": {
		"@jobmatchme/bee-dance-core": "0.1.5",
		"@jobmatchme/bee-dance-schema": "0.2.4",
		"nats": "^2.29.3"
	},
	"devDependencies": {
		"@biomejs/biome": "2.3.5",
		"@typescript/native-preview": "7.0.0-dev.20260120.1",
		"@types/node": "^24.3.0",
		"@vitest/coverage-v8": "^3.2.4",
		"husky": "^9.1.7",
		"shx": "^0.4.0",
		"typescript": "^5.7.3",
		"vitest": "^3.2.4"
	},
	"engines": {
		"node": ">=20.0.0"
	},
	"license": "MIT"
}
